import Box from '@mui/material/Box';

import withLayout from '../hoc/withLayout';
import styles from '../styles/Treasury.module.scss';

function Treasury() {
  return (
    <Box>
      Treasury
    </Box>
  );
}

export default withLayout(Treasury);
